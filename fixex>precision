#include <bits/stdc++.h>
using namespace std;
int main(){
int x1,x2,y1,y2;
double len;
cin>>x1>>y1>>x2>>y2;
len = sqrt(pow(x2-x1,2)+pow(y2-y1,2));
cout<<fixed<<setprecision(3)<<len<<endl;
return 0;
}
